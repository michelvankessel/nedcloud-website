# Database Configuration
# These values are used by Docker Compose and the application
DB_USER=nedcloud
DB_PASSWORD=change_me_in_production
DB_NAME=nedcloud
DB_PORT=5432

# Full database URL (used by Prisma)
# For Docker: postgresql://nedcloud:password@postgres:5432/nedcloud
# For local: postgresql://nedcloud:password@localhost:5432/nedcloud
DATABASE_URL=postgresql://nedcloud:change_me_in_production@localhost:5432/nedcloud

# NextAuth Configuration
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=replace_with_secure_random_string
NEXTAUTH_URL=http://localhost:3000

# Admin User (for database seeding)
# These are used when running: npx prisma db seed
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=change_me_to_strong_password

# Instructions:
# 1. Copy this file to .env.local
# 2. Change all passwords to secure values
# 3. Generate NEXTAUTH_SECRET with: openssl rand -base64 32
# 4. For Docker usage, database host is 'postgres' not 'localhost'
# 5. Never commit .env.local to git (it's already in .gitignore)